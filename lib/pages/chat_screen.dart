part of 'pages.dart';

class ChatScreen extends StatelessWidget {
  // TODO : Buat chat screen, bagaimana ?
  final Doctor doctorReceiver;
  ChatScreen(this.doctorReceiver);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
